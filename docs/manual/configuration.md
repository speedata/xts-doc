* [TOML format](https://toml.io/en/)

Example:

```toml title="publisher.cfg"
filter="filter.lua"

[variables]
foo = "bar"
bar = 123
```

Allowed keys:

data
dummy
jobname
layout
loglevel
filter
quiet
systemfonts
verbose
trace
variables